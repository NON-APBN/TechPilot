name: techpilot
description: "TechPilot – Flutter Web style app (AI Assistant, Rekomendasi, Jelajah, Bandingkan)"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  intl: ^0.20.2
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  csv: ^5.1.1
  sizer: ^2.0.15
  rxdart: ^0.27.7
  flutter_animate: ^4.5.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Folder aset gambar yang dipakai oleh data kamu:
  assets:
    - assets/images/
    - assets/images/Profile/
    - backend/data/
